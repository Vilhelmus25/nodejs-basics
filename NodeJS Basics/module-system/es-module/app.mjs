import * as utils from "./utils.mjs"        // mindent importálunk

//import { even, odd } from './utils.mjs'       // mi adjuk meg

const priceList = [1231, 53456, 71245, 23451, 210, 13245,]
console.log(utils.even(priceList))
console.log(utils.odd(priceList))